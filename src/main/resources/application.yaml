spring:
  datasource:
    url: jdbc:mysql://localhost:3306/alessiodata
    username: root
    password: Alexp896!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create
  jackson:
    serialization:
      fail-on-empty-beans: false
